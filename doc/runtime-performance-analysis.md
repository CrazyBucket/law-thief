# 《窃律者》运行时性能分析

日期：2026-07-11，2026-07-12 更新
结论状态：已完成静态审计、同机运行采样、四批优化与回归验证；单位/前景绘制进一步分层仍待后续处理。

## 1. 结论摘要

当前“玩起来卡卡的”主要不是战斗规则复杂度或硬件不足，而是战斗表现层在高刷新率下持续做了过多重复工作。

最关键的两个问题是：

1. **棋盘静止时仍每帧整板重绘。** `IsometricBoard` 把地块、叠加层、单位、血条、意图、宝石、选区和特效都放在同一个 `_draw()` 中。当前行动单位、玩家待机动画或任意草地/状态叠加层都会让它每帧 `queue_redraw()`，从而反复重建整个 8×8 棋盘的 Canvas 绘制命令。
2. **攻击模式下，每次鼠标移动都会同步重算完整攻击范围和命中预览。** 同一格内的鼠标移动也没有去重。实测单次攻击高亮查询约 **5.6 ms**；本机 160 Hz 显示器每帧预算只有 **6.25 ms**，再叠加棋盘重绘后会直接错过刷新周期，表现为鼠标移动、选目标时明显不顺滑。

复杂草地、毒雾和烟雾关卡还会启用 6 个持续更新的 `SubViewport`，但 A/B 结果表明它们是次要成本；真正占大头的是棋盘 `_process()` 引发的整板重绘和攻击预览重算。

此外，进入战斗存在约 **0.4–0.8 秒同步初始化**和 **42–87 ms 首帧尖峰**，会造成切场景时停一下。战斗事件播放器中的串行等待则会带来“节奏慢”的体感，但它和实际掉帧应分开处理。

## 2. 测试环境与方法

测试机器就是当前项目所在电脑：

- CPU：AMD Ryzen 7 9700X，8 核 16 线程
- GPU：NVIDIA GeForce RTX 5070 Ti
- 内存：约 48 GB
- 主显示器：3840×2160，160 Hz
- 引擎：Godot 4.6.3 Stable
- 渲染器：OpenGL 3.3 Compatibility
- 项目渲染方式：`gl_compatibility`

采样使用实际窗口渲染，关闭 VSync 和 FPS 上限以暴露真实帧耗时；每个稳态阶段预热 120 帧、采样 360 帧。对照组只在诊断脚本中临时停用棋盘 `_process()` 或停用叠加层 `SubViewport`，没有修改游戏实现。

因此本文数据适合判断热点和优化优先级，不应当作发布版的最终 FPS 承诺。Debug 构建、后台程序和驱动调度会造成一定波动。

## 3. 关键采样结果

### 3.1 1600×900 稳态 A/B

| 场景 | 默认平均帧耗时 | 停止棋盘 `_process()` | 完全静态 | 判断 |
|---|---:|---:|---:|---|
| 教学战 `tutorial_001` | 约 2.9 ms | 约 0.68 ms | 约 0.68 ms | 约 76% 的帧时间来自棋盘持续更新/重绘 |
| 复杂叠加层 `toxic_furnace` | 约 4.0 ms | 约 0.95 ms | 约 0.73 ms | 整板重绘仍是主体，6 个 SubViewport 约再增加 0.2 ms |

`toxic_furnace` 默认约 226 个 draw calls、765 个渲染对象；教学战约 175 个 draw calls、630 个渲染对象。对一个 8×8 回合制棋盘来说，这不是 GPU 顶点量问题，而是单一 CanvasItem 反复生成大量细碎绘制命令的问题。

### 3.2 4K / 160 Hz 实机结果

代表性复测结果：

| 场景 | 平均 | P95 | P99 | 160 Hz 帧预算 |
|---|---:|---:|---:|---:|
| 教学战 | 4.29 ms | 5.79 ms | 6.55 ms | 6.25 ms |
| `toxic_furnace` | 4.36 ms | 5.82 ms | 6.67 ms | 6.25 ms |

P99 已经超过 160 Hz 的 6.25 ms 帧预算。另一轮采样中教学战 P95 达到 9.82 ms、最大帧 22.61 ms，说明在后台负载或资源调度稍有扰动时会连续错过高刷刷新周期。高配置电脑并不能消除这种主线程重复工作的结构性开销。

### 3.3 鼠标高亮查询

每种模式循环查询 512 次、轮换 64 个格子的平均耗时：

| 模式 | 教学战单次平均 | `toxic_furnace` 单次平均 |
|---|---:|---:|
| 无动作 | 0.0048 ms | 0.0058 ms |
| 移动 | 1.01 ms | 0.85 ms |
| 攻击 | 5.70 ms | 5.58 ms |

攻击模式的单次查询已经接近整个 160 Hz 帧预算。鼠标移动时还会触发棋盘高亮更新和整板绘制，所以这是最符合“操作时卡卡的”描述的直接根因。

### 3.4 切入战斗

4K 采样中观察到：

- 战斗场景冷加载：约 0.53–0.63 秒；
- 场景 `_ready()` 同步初始化：约 0.40–0.79 秒；
- 第一帧：教学战约 42 ms，复杂叠加层关卡约 87 ms。

这是切场景停顿，不是稳态 FPS 低。复杂关卡首次出现新敌人时，档案解锁还会同步写盘，多次写入可能放大这次尖峰。

## 4. 根因定位

### P0：棋盘每帧整板重绘

证据位置：

- `scripts/ui/isometric_board.gd:272-326`：棋盘永久启用 `_process()`。
- `scripts/ui/isometric_board.gd:277-278`：只要存在当前行动单位，就把画面标记为 dirty。
- `scripts/ui/isometric_board.gd:288-294`：玩家和史莱姆待机动画每帧推进，也会让整板 dirty。
- `scripts/ui/isometric_board.gd:329-337`：每帧遍历全部地块判断是否存在动画叠加层。
- `scripts/ui/isometric_board.gd:414-432`：每帧再次遍历地块，并为 SubViewport 重设更新模式。
- `scripts/ui/isometric_board.gd:599-643`：一次 `_draw()` 重画地块、实体、掉落物、单位、单位 UI、特效和槽位面板。
- `scripts/ui/isometric_board.gd:595-596` 与 `scripts/map/iso_coordinates.gd:183-198`：`_draw()` 每次多次调用 `_sorted_cells()`；每次都重新生成 64 格数组并执行排序。

这里的核心问题不是某个 draw call 特别昂贵，而是架构把静态和动态内容绑定在同一个 CanvasItem 上。一个只需要更新的行动光环或 10 FPS 待机帧，会迫使整个棋盘重建绘制命令。

### P0：鼠标移动触发重复的攻击预览计算

证据位置：

- `scripts/ui/board_input_adapter.gd:26-34`：每个 `InputEventMouseMotion` 都发出 `cell_hovered`，没有“格子未变化则返回”的保护。
- `scripts/ui/battle_scene.gd:617-647`：每次 hover 都调用 `_controller.get_highlights(_hover_cell)`。
- `scripts/battle/battle_query_service.gd:26-109`：每次都重新创建整套结果字典并计算动作范围、路径、效果和意图叠加层。
- `scripts/battle/battle_query_service.gd:353-366`：攻击范围每次扫描整个 8×8 棋盘。
- `scripts/battle/battle_query_service.gd:393-436`：再针对当前格计算分裂、爆炸、光束和死亡宝石预览。

这些结果在同一个战斗状态、同一个动作下大部分是不变的，却没有状态版本缓存。高轮询率鼠标和高刷显示器会让这个问题更明显。

### P1：叠加层使用多个持续更新的 SubViewport

证据位置：

- `scripts/ui/isometric_board.gd:340-411`：草地、毒雾、烟雾分别创建独立 SubViewport 和 ShaderMaterial。
- `scripts/ui/isometric_board.gd:414-422`：活跃路径使用 `SubViewport.UPDATE_ALWAYS`。

复杂关卡会同时启用 6 个 SubViewport。它们的直接 GPU 成本在本机约为每帧 0.1–0.2 ms，当前不是第一优先级；但它们增加显存、渲染对象和同步面，低端 GPU 上会更突出。现有美术规范要求保留草地摆动、雾气漂移和前后两段遮挡语义，优化时应复用共享图集或较少的 Viewport，而不是直接删除动画。

### P1：战斗初始化包含同步资源和存档工作

证据位置：

- `scripts/ui/isometric_board.gd:699-738`：每个棋盘实例通过 `Image.load_from_file()` 同步读取四张水体生成图。运行日志明确警告这种加载方式不适用于导出，应改为导入资源后正常 `load/preload`。
- `scripts/ui/doodle_unit_sprites.gd:63-77`、`female_adventurer_sprites.gd:113-125`、`slime_sprites.gd:83-97`：实例级缓存配合 `ResourceLoader.CACHE_MODE_IGNORE`，新棋盘会绕过全局资源缓存重新加载素材。
- `scripts/ui/female_adventurer_sprites.gd:88-110` 与 `slime_sprites.gd:100` 之后：首次使用每个动画帧时会从纹理取 Image、逐像素扫描包围盒并创建 AtlasTexture。
- `scripts/services/profile_service.gd:14-20`、`144-163`：每个首次解锁标记都会立即同步写 `profile.json`，并更新存档槽元数据；同一战斗出现多个新敌人时会连续写盘。

### P1：HUD 在状态刷新时反复销毁并创建控件

证据位置：

- `scripts/ui/battle_scene.gd:2505-2552`：一次状态刷新会更新整个 HUD、重新取高亮并安排多次延迟布局。
- `scripts/ui/battle_hud_presenter.gd:430-460`：检查面板先 `queue_free()` 所有槽位控件再重建。
- `scripts/ui/battle_hud_presenter.gd:856-869`：时间轴每次刷新全部销毁再重建。

这不会每帧发生，但会在移动、攻击、切换动作和回合推进时制造分配、布局与释放尖峰，使“点一下后顿一下”的体感更明显。

### P2：主菜单也保持持续刷新

证据位置：

- `scripts/main/main_root.gd:65-71`：主菜单每帧更新 56 个字典粒子并重画粒子 Canvas。
- `scripts/ui/pixel_menu_button.gd:43-56`：5 个菜单按钮无论是否 hover 都每帧更新 Shader 参数并 `queue_redraw()`。
- `scenes/main/main_menu_background.gdshader`：4 层 FBM 全屏动态雾效。

本机主菜单约 1.7 ms，暂时不是战斗卡顿主因，但在核显、节能模式或 4K 笔记本上会造成无必要的功耗和风扇负载。

### 体感问题：事件播放包含有意的串行等待

`scripts/ui/battle_event_player.gd` 会按 beat 串行播放，普通伤害、区域效果、爆炸恢复等含 0.2–0.75 秒等待。大量事件虽然 FPS 可能正常，仍可能让玩家觉得反馈拖沓。项目已有 0.75×–2.0×战斗动画速度设置，因此应先解决真实掉帧，再用批处理和节奏调整处理这一层体感，避免把两类问题混为一谈。

## 5. 建议的优化顺序

### 第一阶段：先解决两项 P0

1. **拆分棋盘绘制层。** 至少拆成静态地形层、地块叠加层、单位层、单位 UI 层、高亮/路线层、VFX 层。静态地形只在状态或尺寸变化时重画；行动光环只重画自己的小层；单位待机动画以素材真实帧率更新单位层，而不是驱动整板 160 FPS 重画。
2. **移除 `active_turn_unit_uid` 导致的无条件整板 dirty。** 行动光环改成独立 CanvasItem/节点，或只在 30/60 Hz 更新光环层。
3. **缓存排序格子和地块动画签名。** 棋盘尺寸或 origin 变化时才重建 `_sorted_cells`；地块状态变更时才重算 active overlay paths。
4. **鼠标 hover 按格去重。** `pick_cell()` 结果与上一次相同时直接返回，不再计算高亮。
5. **缓存动作高亮。** 以 `state revision + selected_action + player uid` 为 key 缓存 reachable/attack range/静态 overlay；鼠标变化只算当前格专属的路径或命中预览。
6. **一次构建宝石上下文。** 攻击范围扫描前构建一次红槽上下文，不要在 64 格循环中重复查询相同的光、重力、分裂和爆炸状态。

第一阶段验收目标：

- 教学战静止帧在 4K 下 P95 小于 3 ms；
- 攻击模式 hover 查询小于 0.5 ms；
- 同一格内移动鼠标不触发查询和重绘；
- 4K/160 Hz 下教学战与复杂叠加层战斗 P99 均低于 6.25 ms，并保留至少 30% 余量。

### 第二阶段：处理切场景和操作尖峰

1. 水体生成图改用导入后的 Texture/Image 资源，不在每个棋盘 `_ready()` 中直接读文件。
2. 单位、宝石、动画帧资源使用共享缓存；移除正常运行路径中的 `CACHE_MODE_IGNORE`。
3. 在地图进入战斗前异步预热战斗场景和本关单位素材。
4. Profile 解锁改为内存批量记录，在场景稳定后或固定时机一次落盘。
5. 时间轴、槽位、状态图标改为节点复用或按差异更新，避免整组 `queue_free + new`。

第二阶段验收目标：战斗冷切入小于 200 ms，首个可见帧小于 16.7 ms；重复进入战斗不应出现同步资源读取警告。

### 第三阶段：图形选项与低端适配

1. 增加 60/120/无限 FPS 档和 VSync 选项。当前项目没有显式帧率上限，高刷屏会把主线程压力放大。
2. 增加“叠加层动画质量”设置：高档保留当前连续 shader，中档降更新频率，低档使用静态帧；保持叠加层的玩法可读性和前后遮挡语义。
3. 主菜单在无 hover、无过渡时降低粒子和按钮更新频率；必要时为全屏 FBM 提供低质量路径。

## 6. 玩家侧临时缓解办法

在后续优化完成前，可用以下方式进一步减轻体感：

- 暂时使用窗口模式，避免以 4K 全屏运行；
- 如果显卡控制面板支持，为该程序临时限制到 60 或 120 FPS，并保持稳定 VSync；
- 战斗动画速度调到 1.5× 或 2.0×只能改善串行动画的等待感，不能修复鼠标攻击预览和整板重绘导致的掉帧。

## 7. 建议新增的性能回归门禁

建议增加独立的性能探针，不混入普通语义测试：

- `battle_idle_frame_probe`：教学战与 `toxic_furnace` 各采样 600 帧；
- `battle_hover_query_probe`：分别测 idle/move/attack 64 格循环；
- `battle_scene_cold_start_probe`：记录 load、instantiate、ready、first visible frame；
- 固定输出平均、P95、P99、最大帧、draw calls、渲染对象、节点数；
- 性能结果写入 `artifacts/verify/performance.json`，只在专用机器上设硬门槛，普通开发机保留趋势对比。

## 8. 最终判断

本项目的卡顿是可定位、可修复的，且不需要降低核心玩法复杂度。优先改掉“每帧整板重绘”和“每次鼠标移动重算攻击预览”后，当前硬件应能非常轻松地稳定运行；复杂叠加层、资源预热和 HUD 节点复用属于随后收益明确的第二批工作。

## 9. 首批优化实施记录（2026-07-12）

本轮完成以下改动：

1. `BoardInputAdapter` 记录上一次逻辑格，同一格内的连续 MouseMotion 不再重复发出 `cell_hovered`；单位槽位 hover 仍逐事件更新，避免同格内不同槽位失去响应。
2. `BattleQueryService` 缓存移动可达范围和攻击范围；移动路径、命中范围、致死预测和黑槽死亡效果仍按当前 hover 格实时计算。
3. Controller 在状态变更和无 `state_changed` 的移动、攻击、主动触发完成后显式清理范围缓存；缓存 key 同时包含 state、玩家、位置、范围和编辑器无限行动状态作为防御。
4. 攻击范围扫描前只计算一次光宝石状态，不再对 64 个格重复查询相同条件。
5. 棋盘环境型连续重绘合并到最高 60 Hz；交互 setter、移动 Tween、投射物 Tween 和显式 VFX 仍可即时请求重绘。
6. `_sorted_cells()` 按棋盘尺寸和 origin 方向缓存，避免一次 `_draw()` 内四次重新创建并排序 64 格数组。
7. `set_hover`、`set_timeline_hover_unit`、`set_active_turn_unit` 对相同值直接返回，减少重复失效。

同机 1600×900、Compatibility、关闭 VSync、预热后复测：

| 指标 | 优化前 | 首批优化后 | 变化 |
|---|---:|---:|---:|
| 移动 hover 查询平均 | 约 0.85–1.01 ms | 0.027 ms | 约降低 97% |
| 攻击 hover 查询平均 | 约 5.58–5.70 ms | 0.029 ms | 约降低 99.5% |
| 稳态平均帧耗时 | 约 2.9 ms | 0.99 ms | 约降低 66% |
| 720 个渲染帧中的棋盘重建 | 原逻辑接近逐帧 | 46 次 | 环境动画不再跟随无限帧率重建 |

复测 P95 为 2.35 ms、P99 为 3.36 ms；较慢帧主要是保留的实际重绘帧。数值来自诊断运行，不作为跨机器硬门槛。

本轮新增 `board_input_adapter_test.gd`、`battle_query_cache_test.gd`，并扩展 `battle_renderer_timing_test.gd`，覆盖同格去重、缓存防污染、缓存清理、60 Hz 合并和排序缓存。下一批优先处理同步资源加载、Profile 连续写盘以及 HUD 控件反复重建。

验证结果：

- `tools/verify fast`：23/23 通过；
- `tools/verify all`：69 通过、3 失败；3 项与仓库已记录的基线问题一致，分别是 `overlay_render_contract_test`、`water_frame_composite_test`、`water_render_contract_test`，不在本轮优化范围内；
- 可执行宝石语义覆盖：71/90（78.88%）；
- 三枚爆炸宝石的固定战斗快照通过，战斗与事件不变式无违反。

## 10. 宝石负载专项高优优化（2026-07-28）

### 10.1 面向玩家的结论

“携带几十颗宝石后，移动和攻击明显掉帧”并不是 2D 渲染能力不足，也不是上一轮棋盘和 hover 优化失效。根因是每次 HUD 状态刷新都为每个槽位重复构造宝石标签上下文、生成两份提示内容，并销毁后重建整组槽位控件。宝石数增加时，其中的标签、组合和定义查询会放大成超线性 CPU 与分配开销。

本轮在不改变宝石数值、槽位规则或战斗语义的前提下，移除了这条热点中的重复计算。50 槽压力场景的同步 HUD 刷新平均耗时从 **2313.331 ms** 降至 **31.328 ms**（降低 **98.65%**，约 **73.8 倍**）；单次红槽标签上下文从 **22.787 ms** 降至 **0.462 ms**（降低 **97.97%**，约 **49.3 倍**）。因此此前会造成数秒卡顿的主要路径已被消除。

31 ms 的“强制全量 HUD 刷新”在 60 Hz 下仍可能丢 1 帧、在高刷显示器下仍可察觉；它不再是多秒冻结，但下一步仍应把槽位控件由整组重建改为按差异复用，以继续压低操作尖峰。

### 10.2 本轮改动

1. `BattleHudPresenter` 在一次 HUD 刷新中按槽位颜色只构建一次标签上下文，并把同一槽位的效果说明同时复用给文本提示和结构化提示；此前每颗已装配宝石会分别构建两次。
2. `DataRegistry` 的内部只读查询不再为每次标签、等级和组合读取深拷贝整份宝石定义；公开 `get_gem_def()` 仍返回独立深拷贝，保持调用方可修改副本的既有契约。
3. `GemTagResolver` 将“每个标签对反复扫描全部宝石”改为“一次扫描建立最大叠层与组合索引，再解析标签对”，复杂度由随标签对重复扫描宝石缩减为一次宝石扫描加标签对判断。
4. `BattleScene` 去除选择动作、成功移动/攻击播放结束后由状态信号已触发的重复 HUD 刷新，避免一次操作连续刷新两遍。

### 10.3 同机量化结果

使用新增的 `scripts/tests/manual/gem_load_performance_test.gd`：在实际战斗场景中，将 5、20、50 颗固定宝石装配到玩家红槽；每档测量 8 次同步 HUD 刷新（平均值与 P95），并测量 40 次红槽标签上下文构建。优化前与优化后均使用同一台机器、同一探针与同一渲染配置；结果用于同机趋势对比，不作为跨机器 FPS 承诺。

| 装配槽数 | HUD 平均：优化前 → 后 | HUD P95：优化前 → 后 | 标签上下文：优化前 → 后 |
|---:|---:|---:|---:|
| 5 | 12.570 → 6.480 ms（-48.45%） | 13.240 → 6.985 ms | 0.640 → 0.111 ms（-82.72%） |
| 20 | 410.893 → 17.082 ms（-95.84%） | 426.490 → 18.830 ms | 9.621 → 0.236 ms（-97.55%） |
| 50 | 2313.331 → 31.328 ms（-98.65%） | 2329.344 → 33.651 ms（-98.56%） | 22.787 → 0.462 ms（-97.97%） |

最终探针日志：

```text
GEM_LOAD_PERF slots=5  state_gems=7  refresh_avg_ms=6.480  refresh_p95_ms=6.985  context_avg_us=110.5
GEM_LOAD_PERF slots=20 state_gems=22 refresh_avg_ms=17.082 refresh_p95_ms=18.830 context_avg_us=235.6
GEM_LOAD_PERF slots=50 state_gems=52 refresh_avg_ms=31.328 refresh_p95_ms=33.651 context_avg_us=462.3
```

### 10.4 验证与已知非本轮问题

- `tools/verify test gem_tag_resolver_test gem_level_context_test` 通过；两项均覆盖标签等级与组合语义。
- `tools/verify manual`：`battle_editor_overlay_catalog_test`、`battle_stress_test`、`gem_load_performance_test` 通过，后者输出上述数据。
- `manual/damage_debug_test` 仍因既有的单位占位不变量错误失败（`unit_bomb_rat_2` 的位移路径）；该错误不涉及 HUD 或宝石标签解析。
- `attack_tag_combo_test` 当前也会失败，因为工作区已有的冰宝石规则改动移除了“攻击者自身缓速”，而测试仍断言旧行为；本轮未改动该规则。

## 11. 第二批运行时与 I/O 优化（2026-08-09）

本轮继续处理第 5 节第二阶段中不涉及玩法语义的热点：

1. 菜单音乐改由 `AudioService` 单一持有并使用 OGG，移除场景内重复播放器和未引用的 19 MB WAV。
2. `ProfileService` 将同一帧内的图鉴、成就和解锁标记合并为一次延迟写盘；切换存档槽和退出前强制刷新脏数据。
3. 转场开始时提交 threaded scene load，让场景读取与遮罩动画重叠；加载失败仍恢复转场状态并返回明确错误。
4. 单位、宝石、道具和棋盘特效纹理由 `CACHE_MODE_IGNORE` 改为共享资源缓存，避免每次进入战斗绕过 Godot 缓存。
5. HUD 槽位和行动顺序增加内容签名：内容未变化时保留现有节点，只更新生命、状态等轻量字段。
6. 主菜单背景更新封顶 60 Hz；像素按钮只在 hover、按压或残留粒子动画期间启用 `_process()`。

聚焦验证覆盖 Profile 批量持久化、菜单音频/设置页、转场生命周期、宝石 HUD 上下文和 UI 编译。`verify manual` 同机最终复测中，50 槽强制 HUD 刷新平均耗时从上一轮的 **31.328 ms** 降到 **1.591 ms**，P95 从 **33.651 ms** 降到 **1.760 ms**；平均耗时再降低约 **94.9%**。5 / 20 / 50 槽平均值分别为 1.370 / 1.515 / 1.591 ms，说明未变化的槽位节点已不再随每次 HUD 刷新重复构造。冷启动耗时仍需后续独立探针量化。

## 12. 第三批棋盘分层与存档可靠性优化（2026-08-09）

1. `IsometricBoard` 将高亮填充、路线、编辑预览、轮廓和地表覆盖层的后景 pass 移入独立 `BattleBoardBackLayer`。鼠标格子变化只刷新该层，不再要求单位、血条、意图和宝石节点重建绘制命令。
2. 后景覆盖层以 30 Hz 更新；单位移动、受击和战斗 VFX 继续使用原有最高 60 Hz 路径。GPU shader 驱动的草地、雾和烟仍连续运动，火焰帧、高亮呼吸等低频视觉则随 30 Hz 后景层更新。
3. 遮挡策略保持既有契约：后景 pass 位于单位之后，低高度前景 pass 仍留在单位/UI 主绘制路径中，避免草尖、雾或火焰覆盖角色面部、意图、血条和状态图标。
4. 棋盘内嵌动画状态抽取为 `BattleBoardAnimationState`，使 `isometric_board.gd` 保持在文件大小门禁内。
5. `ProfileService` 改用 `SaveService.write_json_atomic()`，批量写盘优化之外进一步避免覆盖写中断造成的损坏。
6. 删除两张 SHA-256 相同且无运行时引用的旧标题图，共减少 1,100,180 字节；当前 `main_title_v2.png` 不变。

## 13. 第四批冷启动资源与着色器预热优化（2026-08-09）

新增 `manual/startup_performance_test`，在同一 Godot 进程中分段记录内容目录、战斗状态工厂、战斗脚本、棋盘脚本、场景本体、实例化、首两帧以及后续 8 帧。该探针不设置跨机器硬阈值，只用于同一机器和构建配置下的趋势对比。

本轮将编辑器面板、生成地图导出按钮、奖励视图和结算服务移出普通战斗的冷加载路径；实体贴图、路线箭头和战斗着色器改为首次实际需要时使用共享缓存加载。着色器池仍在玩家操作前完成预热，但从首帧强制 `force_draw` 改为延后两帧、按三组分帧预热，保留批量特效复用契约。

水体仍保留固定的填充、浅水和岸边节点及原有绘制顺序，但无水关卡不再加载约 1.48 MB 的上下水面纹理、五张浅水遮罩、四张生成图和水面材质；首次出现对应水体时才建立资源。浅水同步改以 `GameState.revision + 棋盘变换` 为签名，状态和布局未变化时不再逐帧扫描全部地块。奖励内容构建则抽取为按需加载的 `BattleRewardView`，实际构建宝石、掉落物和遗物视图的契约测试继续覆盖原有文本与节点结构。

同机前后结果：

| 指标 | 优化前 | 优化后 | 变化 |
|---|---:|---:|---:|
| 战斗脚本依赖冷加载 | 328.580 ms | 256.269 ms | -22.0% |
| 棋盘脚本依赖冷加载 | 218.237 ms | 171.064 ms | -21.6% |
| 场景资源合计 | 590.083 ms | 469.825 ms | -20.4% |
| 首帧 | 151.817 ms | 132.085 ms | -13.0% |

最终探针进一步拆出 `board_ready_ms=31.700`、`battle_ready_ms=41.697` 和 `start_battle_ms=34.403`，便于下一轮区分棋盘节点初始化、场景组装与开战状态刷新。分帧预热的后续 8 帧峰值为 6.906 ms、合计 48.279 ms，没有把原 150 ms 级首帧停顿整体搬到单个后台帧。普通教学战仍有约 470 ms 的 GDScript 冷编译和约 132 ms 的首帧成本；下一批应优先拆分 `IsometricBoard` 的单位 UI/战斗特效职责，并继续收窄 `BattleScene` 的开战刷新路径。

## 14. 第五批棋盘首帧像素扫描优化（2026-08-09）

`IsometricBoard._ready()` 原先会读取 1254×1254 的 `tiles.png`，以步长 2 检查约 39 万个像素，寻找非透明、非纯白内容的包围盒。这项工作对固定素材每次都产生相同结果，却占用了约 31 ms 的棋盘初始化时间。

运行时现直接使用经验证的内容区域 `Rect2(70, 76, 1116, 1102)`；如果纹理尺寸不再是 1254×1254，则安全回退到完整纹理范围。`battle_renderer_timing_test` 会按原阈值重新扫描实际 PNG 并与预计算区域比较，因此素材替换后不会静默沿用过期裁剪。开战流程同时移除了 `start_encounter()` 已清空动作并发出状态刷新后，紧接着再次执行的空动作选择。

两次同机复测中：

- `board_ready_ms` 从 31.700 ms 降至 0.416–0.440 ms，降低约 98.6%；
- 首帧从 132.085 ms 降至 101.362–108.267 ms，降低约 18.0%–23.3%；
- 第二帧保持在 1.161–1.313 ms，后续分帧预热峰值保持在 6.908–6.911 ms。

新增的首绘分段显示：棋盘 `_draw()` 首次执行为 16.421 ms，其中地面/尸体/宝石后景 4.078 ms、单位本体 1.952 ms、实体 UI 0.057 ms、单位 UI 10.332 ms。`start_battle` 仍为约 36.7 ms，其中战斗状态工厂约 20–22 ms；下一批应优先处理单位 UI 首次资源准备和战斗状态构建，而不是继续微调已降至亚毫秒的棋盘 `_ready()`。

## 15. 第六批敌人意图寻路优化（2026-08-09）

状态工厂分段确认，普通遭遇创建时间几乎全部消耗在 `IntentSystem.refresh_all_intents()`。通用近战 AI、裂变史莱姆和石弓守卫都会从多个本回合可达位置再次发起完整 A*；石弓守卫还会为每个走位候选重复生成开火锚点，并逐锚点寻路。

本轮保持原候选顺序、完整 footprint、地形代价和加权 A* 结果，只减少不可能影响结果的查询：

1. 通用 1×1 近战 AI 使用“到玩家锚点的曼哈顿距离减去相邻终点一步”作为下界；下界已不优于当前解时，不再启动 A*。
2. 裂变史莱姆使用完整 footprint 到玩家锚点的距离下界，同样只跳过不可能刷新最优解的候选。
3. 石弓守卫在一次决策内只生成一次开火锚点、只计算一次起点到最近开火点的距离。每个走位候选先用无地形代价 BFS 得到真实可通行步数下界，再按下界调用原加权 A*；BFS 不参与最终路径和评分。
4. 棋盘同一绘制帧内复用单位中心、偏移、色调和精灵尺寸上下文，单位本体与单位 UI 不再各计算一次。

同机诊断前后结果：

| 指标 | 优化前 | 优化后 | 变化 |
|---|---:|---:|---:|
| 教学战意图刷新 | 20.954–22.539 ms | 7.279–7.596 ms | 约 -65% |
| `template_a` 意图刷新 | 129.219–132.220 ms | 约 19–20 ms | 约 -85% |
| `template_c` 意图刷新 | 207.101–215.567 ms | 约 20–24 ms | 约 -89% |
| 独立裂变史莱姆意图刷新 | 约 63–71 ms | 约 15–16 ms | 约 -76% |
| 手工战斗压力用例 | 18 s | 6 s | 约 -67% |

移除诊断日志后的启动探针为：`state_factory_ms=8.223`、`start_battle_ms=23.479`、首绘 15.432 ms、首帧 91.188 ms。相比第三批基线，战斗状态工厂从约 20–22 ms 降至 8.2 ms，`start_battle` 从 34.403 ms 降至 23.479 ms；单位上下文复用后首绘中单位本体为 1.262 ms、单位 UI 为 10.127 ms。聚焦 AI/意图契约 5 / 5 与手工场景 5 / 5 均通过。

## 16. 第七批单位 UI 首绘与菜单空闲预热优化（2026-08-09）

单位 UI 子阶段诊断确认，状态图标、宝石、血条等绘制均为亚毫秒级；主要冷成本来自第一次创建 128×128 柔光纹理的 GDScript 逐像素循环，以及第一次同步载入敌人意图 PNG。运行时柔光现在改由原生 `GradientTexture2D` 构建径向渐变，移除首绘中的 16,384 次脚本像素计算。冷探针中单位 UI 从 10.127 ms 降至约 7.2–7.9 ms，节省约 2.2–3.0 ms；首绘约 12.5–14.3 ms。

游戏实际会先停留在主菜单，因此新增 `BattleUiResourceWarmup`：菜单完成基础初始化并等待两帧后，每帧只把一个现有意图图标装入 `IntentIcons` 共享缓存，共 22 个图标；完成后节点关闭处理并自我释放。这样不增加战斗场景的强制冷依赖，也不把全部同步读取集中到单帧。菜单 UI 契约会等待 26 帧并确认预热节点已释放，从而同时覆盖完整图标目录与生命周期。

用一次性诊断分支模拟“主菜单空闲 30 帧后进入教学战”的实际路径，得到：`first_draw_ms=12.431`、`draw_unit_ui_ms=6.664`、`first_frame_ms=76.707`；相较同轮未经过菜单的冷探针首帧约 92–95 ms，下降约 16–18 ms。该诊断分支在测量后已移除，正式代码只保留逐帧预热器。柔光形状和意图图标内容不变，本轮仅调整资源生成与加载时机。

## 17. 第八批依赖边界与战斗线程转场优化（2026-08-09）

静态预载闭包确认，棋盘为了两个很小的表现层需求引入了大块规则依赖：槽位扇区的距离过滤直接调用 `GemRules`，毒雾爆发的邻格与半径枚举直接调用 `BoardUtils`。现在槽位范围由 `BattleController.is_unit_in_slot_action_range()` 通过 Callable 注入棋盘，毒雾视觉改用只包含边界、四邻格与方形半径枚举的 `BoardVisualGeometry`。控制器中的编辑器 CLI/服务也从普通 `_ensure_services()` 移到首次编辑器命令时加载；暂停菜单和离场确认框则在首次打开时才创建。

按显式 `preload()` 递归闭包统计：

| 根脚本 | 优化前 | 优化后 |
|---|---:|---:|
| `isometric_board.gd` | 110 文件 / 24,489 行 | 36 文件 / 7,453 行 |
| `battle_scene.gd` | 48 文件 / 13,854 行 | 45 文件 / 13,153 行 |
| `battle_controller.gd` | 91 文件 / 21,521 行 | 83 文件 / 19,188 行 |

同步冷探针仍为 `total_scene_resource_ms=466.955`，与上一轮 465.230 ms 基本持平；`battle_script_load_ms` 从 251.473 ms 降至 245.895 ms，但 3,000 行以上根脚本及全局类型解析仍占主要成本。因此本轮不把静态闭包缩减宣称为同步冷加载提速，而是把玩家实际路线改为隐藏和提前完成这段工作。

`TransitionManager` 新增幂等 `prefetch_scene()`，主菜单完成两帧基础初始化后在正式渲染客户端发起战斗场景后台请求。普通、精英和 Boss 房间此前直接 `change_scene_to_file(BATTLE_SCENE)`、会在地图点击后同步加载；现在全部进入已有线程转场，使场景加载与 0.38 秒遮罩动画重叠，并可复用菜单阶段尚未取出的请求。无渲染后端的测试/服务器模式跳过 shader 场景预取，避免后台编译渲染资源；转场管理器的线程请求契约仍由无 shader 固定场景验证。

新增架构守卫禁止棋盘重新显式预载 `GemRules`/`BoardUtils`、控制器重新预载编辑器服务，以及冒险服务重新使用同步战斗切场。规则判断、槽位范围、毒雾格集合、编辑器命令和暂停菜单交互均由聚焦测试保持原契约。

## 18. 第九批战斗编辑器视图按需拆分（2026-08-09）

普通战斗仍会解析整个 `battle_scene.gd`，但原文件内约四百行内容只服务于编辑器战斗：左侧资源面板、右侧检查器、落点预览、目标摘要、宝石/遗物列表和状态按钮。现在这些职责集中到 `battle_editor_view.gd`，由 `battle_scene_lazy_resources.gd` 仅在编辑器战斗启用时动态加载。战斗场景保留编辑器会话、状态变更命令和刷新接线，因此本轮不改变任何战斗或编辑器操作语义。

拆分后的规模为：

| 文件 | 优化前 | 优化后 | 变化 |
|---|---:|---:|---:|
| `battle_scene.gd` | 3,277 行 | 2,892 行 | -385 行（-11.7%） |
| `battle_editor_view.gd` | — | 526 行 | 仅编辑器战斗按需解析 |

两次连续的最终启动探针得到 `battle_script_load_ms=223.842–231.027`，上一轮记录为 `245.895–251.473`，趋势下降约 6%–11%；`total_scene_resource_ms=430.082–456.463`，上一轮为 `465.230–466.955`，趋势下降约 2%–8%。这些数字仍受操作系统文件缓存与同机负载影响，不能视为稳定帧时间承诺；可以确定的是普通战斗已不再解析迁出的 385 行编辑器视图实现。

UI 架构守卫新增两项约束：`BattleScene` 不得直接预载编辑器视图，按需资源表必须持续提供该模块的动态入口。编辑器 UI、编辑器 CLI、渲染时序和编译聚焦测试继续覆盖原入口；手工测试 5 / 5 通过。

## 19. 第十批宝石槽位面板按需加载（2026-08-09）

单位周围的环形槽位面板只在玩家选择“插入”或“拔出”宝石时显示，但其扇区绘制、宝石图标、文字、命中测试和 hover 状态此前全部位于 `IsometricBoard`，并直接预载 `BattleSlotPanelLayout`。现在这些职责迁入 `battle_unit_slot_panel_renderer.gd`；棋盘保留 `configure_unit_slot_panels()`、`set_slot_hover()`、`pick_unit_slot()` 和 `clear_unit_slot_panels()` 公开接口，并通过 `battle_board_resources.gd` 在首次非空槽位动作时才创建渲染器。

本轮保持槽位可见性、操作范围、扇区角度、颜色、文字、宝石纹理和命中几何不变。宝石外观依赖通过 Callable 显式注入，按需模块不再隐式读取 `UnitLooks` 自动加载单例。

| 指标 | 优化前 | 优化后 | 变化 |
|---|---:|---:|---:|
| `isometric_board.gd` | 3,477 行 | 3,311 行 | -166 行（-4.8%） |
| 棋盘显式预载闭包 | 36 文件 / 7,453 行 | 35 文件 / 7,237 行 | -1 文件 / -216 行 |
| 按需槽位渲染器 | — | 278 行 | 普通战斗不加载 |

一次启动探针得到 `board_script_load_ms=176.993`，仍处于上一批 166–184 ms 的同机波动区间，因此本轮不宣称可测量的同步加载毫秒收益。确定性收益是普通路径不再解析槽位渲染器及其布局依赖，并且棋盘主脚本离 3,480 行文件门限多出 169 行余量。

`battle_slot_panel_layout_test` 新增真实状态下的渲染器命中与稳定 hover 契约；`battle_renderer_timing_test` 同时确认普通启动不会创建渲染器、选择插入动作后会按需创建并完成实际绘制。UI 架构守卫禁止棋盘重新直接预载该渲染器，并要求棋盘资源表保留动态加载入口；手工场景 5 / 5 通过。

## 20. 第十一批战斗特效分阶段准备（2026-08-09）

`IsometricBoard._ready()` 原先会同步解析并创建光束、投射物、粒子、Shader 对象池、Shader 预热器和特效纹理服务，然后才完成棋盘就绪。现在这些脚本只以字符串路径登记在 `BattleBoardResources`：普通战斗首帧不创建任何战斗特效层，之后按“Shader 池与预热器 → 粒子 → 投射物 → 光束”每帧一个组件逐步准备。

所有公开播放入口仍具有同步兜底：伤害/治疗/宝石闪光只确保粒子组件，投射物只确保投射物组件，光束只确保光束组件，Shader 电弧、爆炸和云雾只确保 Shader 池。即使事件早于后台准备触发，也会在原调用内建立所需节点并保留完成信号、持续时间和对象池语义；不相关的特效组件不会被顺带加载。

| 指标 | 优化前 | 优化后 | 变化 |
|---|---:|---:|---:|
| 棋盘显式预载闭包 | 35 文件 / 7,237 行 | 28 文件 / 6,636 行 | -7 文件 / -601 行（-8.3%） |
| 棋盘脚本加载 | 176.993 ms | 152.005–155.706 ms | 约 -12%–14% |
| `board_ready` | 0.430 ms | 0.185–0.199 ms | 约 -54%–57% |
| 场景资源合计 | 452.172 ms | 428.466–438.989 ms | 约 -3%–5% |
| 后台 8 帧峰值 | 6.914 ms | 8.259–8.629 ms | +1.345–1.715 ms |

后台峰值有所增加，但仍低于 16.7 ms 的 60 FPS 帧预算，且没有把原同步成本整体转移到单帧。`IsometricBoard` 为分阶段生命周期增加了显式确保函数，主文件由 3,311 行回升到 3,365 行；换取的是普通首帧从编译闭包中移除 7 个文件，并允许各特效类型独立加载。后续若继续收口文件规模，应把这些确保函数迁入专用 FX 编排器，而不是重新急切预载组件。

`battle_renderer_timing_test` 现在明确验证首帧特效层为空、首次特效可即时创建、投射物完成信号正常，以及预热后电弧继续复用池节点。三个独立特效组件测试、编译测试和手工场景 5 / 5 均通过；UI 架构守卫禁止棋盘重新预载这六类模块，并要求资源表保留全部动态工厂。

## 21. 第十二批静止战斗重绘节流（2026-08-09）

棋盘此前存在两个长期整板置脏源：只要 `active_turn_unit_uid` 非空就每个进程帧重绘；玩家或史莱姆处于待机状态时，也会在每帧推进 `idle_phase` 后立即重绘。实际素材的玩家待机只有 10 FPS、史莱姆待机只有 2 FPS，因此大量 60 Hz Canvas 命令重建得到的仍是同一张贴图。

现在待机相位仍按真实 `delta` 连续累计，但只有 `floor(phase × authored_fps)` 变化时才标记主棋盘 dirty；不会改变动画速度、循环点或选帧结果。行动光环的正弦相位同样连续推进，整板采样频率改为 30 Hz。移动、攻击、受击、淡入淡出、宝石运动和粒子仍使用原 60 Hz 门限，交互触发的 `queue_redraw()` 也保持即时。

启动探针新增首批资源/Shader 预热结束后的 60 帧稳态观察窗，以实际 `_draw()` 次数除以经过时间得到棋盘重绘频率。两次同机结果为：

| 指标 | 优化前 | 优化后 | 变化 |
|---|---:|---:|---:|
| 静止教学战整板重绘 | 60 Hz 门限持续置脏 | 28.986–31.401 Hz | 约 -48%–52% |
| 后台 8 帧峰值 | 8.259–8.629 ms | 7.918–8.034 ms | 未恶化 |
| `board_ready` | 0.185–0.199 ms | 0.184–0.192 ms | 基本持平 |

`battle_renderer_timing_test` 分别锁定了玩家待机同帧不重绘、跨贴图帧立即重绘、行动光环 30 Hz 去重，以及移动/受击继续使用 60 Hz 去重。手工压力和启动场景 5 / 5 通过。UI 架构守卫要求棋盘持续保留待机帧与光环采样两个节流入口，防止恢复为逐进程帧整板置脏。
